<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Reminder dùng inexact AllowWhileIdle nên không cần SCHEDULE_EXACT_ALARM. -->

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FlashLockApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashLock">

        <activity android:name=".ui.library.LibraryTopicWordsActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.study.StudyFlashcardActivity" />


        <!-- SplashActivity là màn hình khởi đầu -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name=".ui.reset.ResetAccessActivity" />
        <activity android:name=".ui.reset.ResetConfirmActivity" />
        <activity android:name=".ui.reset.SetNewPasswordActivity" />
        <activity android:name=".ui.login.LoginActivity" />

        <!-- Activity học đã bị gỡ bỏ để tránh xung đột với màn hình khóa thật của máy -->

        <activity
            android:name=".ui.lockscreen.LockScreenConfigActivity"
            android:exported="false" />

        <service
            android:name=".service.LockScreenStudyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Refresh từ Firebase mỗi khi user mở khóa máy -->
        <receiver
            android:name=".receiver.LockScreenStudyReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Nhận sự kiện click nút Next/Prev trên notification -->
        <receiver
            android:name=".receiver.LockScreenNotificationReceiver"
            android:exported="false" />

        <!-- Receiver cho tính năng nhắc nhở học tập hàng ngày -->
        <receiver
            android:name=".receiver.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>